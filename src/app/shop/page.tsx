"use client";

// /shop page — Nav auto-opens the shop overlay when it detects this pathname.
// See Nav.tsx useEffect: if (window.location.pathname === "/shop") setActive("shop")
export default function ShopPage() {
  return null;
}
